# План улучшения адаптивности сайта SlimRate

## Обзор проведённого тестирования

**Тестируемые страницы:**
- `index.html` (главная)
- `pricing.html` (тарифы)
- `hardware.html` (оборудование)
- `restaurant.html` (ресторанный POS)
- `sr-falcon.html` (страница продукта)
- `software.html` (программное обеспечение)

**Тестируемые viewport'ы:**
- Mobile: 375×812 (iPhone X)
- Tablet: 768×1024 (iPad)

**Скриншоты сохранены в:** `.playwright-mcp/`

---

## Выявленные проблемы

### 🔴 Критические (высокий приоритет)

#### 1. Таблица сравнения планов на `pricing.html`
**Проблема:** На мобильных устройствах (375px) таблица Compare Plans имеет горизонтальный скролл, но контент слишком мелкий и нечитаемый. Названия колонок обрезаются.

**Решение:**
```css
@media (width <= 480px) {
    /* Превратить таблицу в карточный вид */
    .pricing-comparison__table {
        display: block;
    }
    
    .pricing-comparison__table thead {
        display: none; /* Скрыть заголовки на мобильных */
    }
    
    .pricing-comparison__table tbody {
        display: flex;
        flex-direction: column;
        gap: 16px;
    }
    
    .pricing-comparison__table tr {
        display: flex;
        flex-direction: column;
        background: #f9fafb;
        border-radius: 8px;
        padding: 16px;
    }
    
    .pricing-comparison__table td {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid #e5e7eb;
    }
    
    .pricing-comparison__table td::before {
        content: attr(data-label);
        font-weight: 600;
    }
}
```

**Альтернативное решение:** Горизонтальный скролл с индикатором:
```css
.pricing-comparison__table-wrapper {
    position: relative;
}

.pricing-comparison__table-wrapper::after {
    content: "→ Прокрутите для просмотра";
    display: block;
    text-align: center;
    font-size: 12px;
    color: #6b7280;
    padding: 8px;
}

@media (width > 768px) {
    .pricing-comparison__table-wrapper::after {
        display: none;
    }
}
```

---

#### 2. Карточки тарифов на `pricing.html`
**Проблема:** На мобильных карточки Basic/Premium/Custom идут вертикально, но занимают много места и текст мелкий.

**Решение:**
```css
@media (width <= 480px) {
    .tariff-card {
        padding: var(--component-spacing-md);
    }
    
    .tariff-card .price {
        font-size: 28px;
    }
    
    .tariff-card ul {
        font-size: 14px;
        line-height: 1.6;
    }
}
```

---

### 🟡 Средний приоритет

#### 3. Слайдер Features на `software.html`
**Проблема:** Табы Features используют слайдер, который на мобильных работает корректно, но навигационные точки слишком маленькие для удобного нажатия.

**Решение:**
```css
@media (width <= 768px) {
    .slick-dots li button {
        width: 32px;
        height: 32px;
    }
    
    .slick-dots li button::before {
        font-size: 12px;
    }
}
```

---

#### 4. Форма "Contact us"
**Проблема:** Форма контактов выглядит хорошо, но кнопка Submit на мобильных слишком узкая.

**Решение:**
```css
@media (width <= 480px) {
    .contact-form button[type="submit"],
    .company-form-wrap button.btn-main {
        width: 100%;
        padding: 16px;
    }
}
```

---

#### 5. Hero-секция на страницах продуктов (`sr-falcon.html`)
**Проблема:** Breadcrumb навигация обрезается на узких экранах.

**Решение:**
```css
@media (width <= 480px) {
    .breadcrumb {
        font-size: 12px;
        overflow-x: auto;
        white-space: nowrap;
        -webkit-overflow-scrolling: touch;
    }
}
```

---

#### 6. Footer на всех страницах
**Проблема:** Колонки футера на планшетах (768px) слишком сжаты, ссылки налезают друг на друга.

**Решение:**
```css
@media (width <= 992px) {
    .footer-links {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
    }
}

@media (width <= 480px) {
    .footer-links {
        grid-template-columns: 1fr;
    }
}
```

---

### 🟢 Низкий приоритет

#### 7. Изображения на страницах продуктов
**Проблема:** Изображения продуктов (hardware) иногда не адаптируются идеально под контейнер.

**Решение:**
```css
.product-image img,
.hardware-card img {
    width: 100%;
    height: auto;
    object-fit: contain;
    max-height: 300px;
}

@media (width <= 480px) {
    .product-image img,
    .hardware-card img {
        max-height: 200px;
    }
}
```

---

#### 8. FAQ секция
**Проблема:** На мобильных FAQ аккордеон работает корректно, но отступы можно оптимизировать.

**Решение:**
```css
@media (width <= 480px) {
    .faq-item {
        padding: 12px 16px;
    }
    
    .faq-question {
        font-size: 14px;
        line-height: 1.5;
    }
    
    .faq-answer {
        font-size: 13px;
        line-height: 1.6;
    }
}
```

---

## Рекомендации по системе breakpoints

### Текущая система (из CSS):
- `<= 1280px` - Large Desktop
- `<= 1200px` - Desktop
- `<= 1024px` - Small Desktop / Large Tablet
- `<= 992px` - Tablet
- `<= 768px` - Small Tablet / Large Mobile
- `<= 480px` - Mobile
- `<= 400px` - Small Mobile
- `<= 360px` - Extra Small Mobile
- `<= 340px` - Minimum

### Рекомендуемая унифицированная система:
```css
/* Mobile First Approach */
:root {
    --breakpoint-sm: 480px;   /* Small Mobile */
    --breakpoint-md: 768px;   /* Tablet */
    --breakpoint-lg: 1024px;  /* Desktop */
    --breakpoint-xl: 1280px;  /* Large Desktop */
}

/* Использовать container queries где возможно */
@container (width <= 480px) {
    /* Mobile styles */
}
```

---

## Порядок выполнения работ

### Фаза 1: Критические исправления (1-2 дня)
1. [ ] Переделать таблицу Compare Plans в адаптивный формат
2. [ ] Оптимизировать карточки тарифов для мобильных
3. [ ] Протестировать на реальных устройствах

### Фаза 2: Средние улучшения (2-3 дня)
4. [ ] Улучшить навигационные элементы слайдеров
5. [ ] Расширить кнопки форм на мобильных
6. [ ] Исправить breadcrumb навигацию
7. [ ] Оптимизировать footer

### Фаза 3: Мелкие улучшения (1-2 дня)
8. [ ] Оптимизировать изображения продуктов
9. [ ] Улучшить FAQ секцию
10. [ ] Провести финальное тестирование на всех breakpoints

---

## Инструменты для тестирования

1. **Chrome DevTools** - Device Mode для быстрой проверки
2. **Playwright** - автоматизированное тестирование (уже настроен)
3. **BrowserStack/LambdaTest** - тестирование на реальных устройствах
4. **Lighthouse** - аудит производительности и доступности

---

## Контрольный чеклист перед релизом

- [ ] Все страницы протестированы на 375px (iPhone SE)
- [ ] Все страницы протестированы на 414px (iPhone Pro Max)
- [ ] Все страницы протестированы на 768px (iPad)
- [ ] Все страницы протестированы на 1024px (iPad Pro)
- [ ] Горизонтальный скролл отсутствует на всех страницах
- [ ] Все интерактивные элементы имеют размер минимум 44×44px
- [ ] Текст читаем без необходимости зума
- [ ] Формы удобны для заполнения на мобильных
