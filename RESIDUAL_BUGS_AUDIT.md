# Аудит остаточных проблем после миграции брейкпоинтов

## Дата: Аудит после Phase 1-5

---

## ✅ ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ

### 1. equip allinone — скачок на 1023→1024
- **Описание:** Блок резко менял размер при переходе с таблетки на десктоп
- **Причина:** clamp() с vw не достигал максимума до брейкпоинта
- **Решение:** Изменен vw с 38vw на 47vw, max достигается на ~1030px

### 2. equip allinone — скачок на 1200→1201  
- **Описание:** Правая часть блока скачала при ширине больше контейнера
- **Причина:** vw продолжал расти пока контейнер уже достиг max-width
- **Решение:** На desktop используются фиксированные значения (484px)

---

## ⚠️ НАЙДЕННЫЕ ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

### 1. hardware-nav: скачок min-height на брейкпоинте (УМЕРЕННЫЙ РИСК)

**Файл:** [hardware-nav.css](docs/assets/css/hardware-nav.css#L44)

```css
/* Базовые стили (до 1023px) */
.hardware-nav__card {
    min-height: clamp(180px, 30vw, 360px);  /* При 1023px: ~307px */
}

/* Desktop 1024px+ */
@media (min-width: 1024px) {
    .hardware-nav__card {
        min-height: 260px;  /* Скачок ~307px → 260px = -47px */
    }
}
```

**Анализ:** Это может быть **намеренным** — на десктопе карточки становятся горизонтальными и им не нужна такая высота. Требуется визуальная проверка.

**Дубликат в:** [style.css#L5653](docs/assets/css/style.css#L5653)

---

### 2. hero.css: дублирующиеся стили внутри одного медиа-запроса (МУСОР)

**Файл:** [hero.css](docs/assets/css/hero.css#L436-L554)

Внутри `@media (max-width: 767px)` одни и те же селекторы определяются несколько раз:

| Селектор | Количество определений |
|----------|----------------------|
| `.mainPageHeader` | 4 раза |
| `.mainPageHeader .container` | 3 раза |
| `.main .block-left` | 4 раза |
| `.main .section-title` | 3 раза |
| `.main-allPages` | 3 раза |

**Проблема:** Это мусор от неаккуратного слияния старых брейкпоинтов. Последнее значение перезаписывает предыдущие.

**Итоговые значения (что применяется):**
```css
.mainPageHeader {
    margin: 0 4px;
    border-radius: 16px 0;
}
.mainPageHeader .container {
    min-height: 300px;
}
.main .block-left {
    padding: 10px;
    border-radius: 0 8px 8px 0;
}
.main .section-title {
    font-size: 20px;
}
```

**Рекомендация:** Удалить дублирующиеся правила, оставить только последние.

---

### 3. breakpoints.css: мусорные правила для .container (МУСОР)

**Файл:** [breakpoints.css](docs/assets/css/breakpoints.css#L272-L280)

```css
@media (min-width: 1024px) {
    .container {
        max-width: 960px;   /* Перезаписывается */
    }
    .container {
        max-width: 1160px;  /* Перезаписывается */
    }
    .container {
        max-width: 1320px;  /* Только это применяется */
    }
}
```

**Рекомендация:** Удалить первые два правила.

---

### 4. clamp() с vw в контейнерах — потенциальные скачки (НИЗКИЙ РИСК)

| Файл | Строка | Значение | vw=max при | Риск |
|------|--------|----------|------------|------|
| hardware-nav.css | 44 | `clamp(180px, 30vw, 360px)` | 1200px | ⚠️ На границе |
| hardware-nav.css | 232 | `clamp(220px, 30vw, 320px)` | 1067px | ✅ OK |
| hardware-nav.css | 269 | `clamp(200px, 35vw, 320px)` | 914px | ✅ OK |
| style.css | 1179 | `clamp(100px, 18vw, 320px)` | 1778px | ⚠️ Не достигнет max |
| style.css | 1180 | `clamp(100px, 22vw, 320px)` | 1455px | ⚠️ Не достигнет max |

**Примечание:** Эти значения используются для картинок и могут быть намеренными — изображения масштабируются пропорционально vw и никогда не достигают максимума.

---

## 📋 РЕКОМЕНДАЦИИ ПО ОЧИСТКЕ

### Критичность: НИЗКАЯ
Найденные проблемы — это в основном мусорный код, не вызывающий визуальных багов.

### Действия:

1. **hero.css** — очистить дублирующиеся правила в `@media (max-width: 767px)`
   - Удалить 12 лишних блоков
   - Оставить только итоговые значения

2. **breakpoints.css** — удалить дублирующиеся `.container` правила
   - Строки 272-274 и 275-277 — удалить

3. **hardware-nav** — проверить визуально переход 1023→1024
   - Если скачок заметен, добавить transition или изменить vw

4. **style.css** — удалить дубликаты hardware-nav (строки 5576-5665)
   - Если hardware-nav.css подключен отдельно

---

## ✅ ПОДТВЕРЖДЁННЫЕ ИСПРАВЛЕНИЯ

| Файл | Проблема | Статус |
|------|----------|--------|
| equip.css | clamp 47vw + fixed desktop | ✅ Исправлено |
| style.css | allinone sync | ✅ Синхронизировано |
| more.js | Slick 1023px | ✅ Исправлено |
| JS components | innerWidth checks | ✅ Исправлено |

---

## ОБЩИЙ ВЫВОД

После миграции на 3-брейкпоинтную систему **критических визуальных проблем не обнаружено**.

Найден только мусорный код (дублирующиеся правила) который не влияет на отображение, но увеличивает размер CSS файлов.

Рекомендуемое время на очистку: 30-60 минут.
